file:
  name: _posts/{{ message.date }}-{{ message.subject }}.md
  content: |
    ---
    layout: post
    ---
    {{ message.body }}
git:
  repo: test.git
  remote: origin
  commit:
    email: mail2git@cplug.org
    name: mail2git
